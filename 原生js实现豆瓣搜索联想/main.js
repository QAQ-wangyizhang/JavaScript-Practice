var obj = {
    el : document.getElementById('app'),
}

var search = {

    initDate : function (options) {
        thisel = options.el;
        this.oInputDon = this.el.document.getElementsByTagName('input');
    },
    handle : function () {
        this.handleInput();
        this.handleFocus();
        this.handle
    }
}